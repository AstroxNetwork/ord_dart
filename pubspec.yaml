name: ord_dart
version: 0.0.8+4

description: "The Flutter plugin that supports Ordinals protocol (Runes protocol only)."
repository: https://github.com/AstroxNetwork/ord_dart
issue_tracker: https://github.com/AstroxNetwork/ord_dart/issues

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_rust_bridge: '>=2.10.0 <2.10.1'
  freezed_annotation: ^3.0.0
  meta: ^1.12.0

dev_dependencies:
  flutter_lints: any
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  build_runner: ^2.4.4
  ffi: ^2.1.0
  ffigen: ^18.0.0
  freezed: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        ffiPlugin: true
      ios:
        ffiPlugin: true
      linux:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      windows:
        ffiPlugin: true
